[project]
name = "repository-explorer"
version = "0.1.0"
description = "Repository Explorer for RCIS Platform"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]